apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shadmin
  namespace: shadmin
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "1Mi"
  volumeName:
  storageClassName: nfs-csi